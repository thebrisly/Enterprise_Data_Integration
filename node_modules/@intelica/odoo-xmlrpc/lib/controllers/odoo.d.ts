import { IServerVersion } from "../models/odoo.types";
import { Contacts } from "./contacts.odoo";
import { Employees } from "./employees.odoo";
import { Leads } from "./leads.odoo";
import { Categories } from "./categories.odoo";
export declare class Odoo {
    private _client;
    private _uid;
    private _db;
    private _password;
    constructor(host: string, port?: number);
    private _commonPath;
    private _objectPath;
    version(): Promise<IServerVersion>;
    authenticate(db: string, username: string, password: string): Promise<number>;
    callRPC(params: any[]): Promise<any>;
    getModelActions(model: string): Contacts | Leads | Employees | Categories;
}

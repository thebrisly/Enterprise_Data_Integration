import { Odoo } from "./odoo";
export declare class OdooCRUD<T> {
    private _model;
    private _odoo;
    protected _keys: string[];
    constructor(model: string, odoo: Odoo, keys?: string[]);
    count(): Promise<number>;
    create(value: T): Promise<number>;
    searchRead(query: any[], limit?: number): Promise<T[]>;
    read(ids: number[]): Promise<T[]>;
    update(id: number, data: T): Promise<boolean>;
    delete(id: number): Promise<boolean>;
}
